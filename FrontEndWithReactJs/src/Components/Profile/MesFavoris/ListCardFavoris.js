/*import React from "react";
import ReactDOM from "react-dom";
import "antd/dist/antd.css";
import { Card, Col, Row } from "antd";
import CardItem from "./cardFavoris";




 import PropTypes from "prop-types";
import { withStyles } from "@material-ui/core/styles";
 class listCardClass extends React.Component {
  
  render() {
     
    return (
      <div style={{ paddingLeft: "10%" }}>
      <Row gutter={16}>
      { this.props.passedValue.map(element  => <Col span={8}>{element.id} </Col>)}

        <Col span={8}>
        <CardItem   />
        </Col>
        <Col span={8}>
          <CardItem  />
        </Col>
        <Col span={8}>
          <CardItem   />
        </Col>
      </Row>
    </div>
  
    );
  }
}

listCardClass.propTypes = {
  classes: PropTypes.object.isRequired
};

export default withStyles()(listCardClass);
*/